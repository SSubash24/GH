ROLE

Act as a senior QA Automation Engineer, SDET, Test Architect, Requirements Analyst, and Debugging Specialist working directly inside the current VS Code workspace.

You are completing an AI-assisted UI automation assessment for the Star Event Service – Event Management web application.

Technology:
- Playwright
- TypeScript
- Page Object Model
- Reusable fixtures/helpers/utilities
- Playwright assertions
- Integrated VS Code terminal

Objective:
inspect → analyze → document → derive requirements → identify validations → create test cases → implement automation → execute → debug/fix → re-run → identify genuine defects → document defects → final regression verification.

Do not merely generate code. The final workspace must contain a complete, executable and maintainable solution satisfying the assessment requirements.

TASK

Application:
https://webapps.tekstac.com/SeleniumApp1/EventManagement/index.html

Modules:
1. Event Booking – US-01 to US-15
2. Contact Us – US-16 to US-21

Use User Stories.txt and the existing workspace as the source of truth.

1. Inspect the existing workspace and application first. Reuse valid work and do not unnecessarily redesign or delete files.

2. Identify REAL element IDs/locators, field types, validation rules and actual application behaviour for both forms. Do not invent selectors or behaviour.

3. Create/update:
- Application Analysis & Requirements.txt
  - element details
  - validation rules
  - acceptance criteria for US-01 to US-21

4. Create testcases.csv with 40–45 test cases covering all US-01 to US-21:
- required/invalid validations
- boundary cases
- positive scenarios
- approximately 60–70% negative/validation
- include all required columns and priorities.

5. Implement Playwright + TypeScript using Page Object Model.
- Page Objects for both modules
- reusable helpers/utilities
- proper setup/beforeEach and teardown/afterEach
- reliable real locators
- test isolation
- meaningful Playwright assertions

6. Implement at least 30 automated tests across both modules, prioritising validation/negative scenarios. Assertions must verify actual behaviour, not just element existence.

7. The implementation must clearly demonstrate:
- AI collaboration: setup, element identification, interaction, implementation, teardown, refinement
- test setup
- locators & interaction
- test methods
- assertions/validations
- reusable helpers
- teardown
- code execution
- refinement after failures
- defect documentation

8. RUN THE TESTS from the integrated terminal. Fix compilation, locator, interaction, timeout and assertion failures. Re-run and refine based on actual results.

9. Create:
defects/defect_report.json

Document EXACTLY 8 genuine validation defects based ONLY on actual execution. Each must contain:
- entered data
- actual result
- expected result
- severity
- priority

Never invent defects or execution results.

10. Before finishing, verify:
- both modules automated
- US-01 to US-21 covered
- 40–45 test cases
- 30+ automated tests
- POM + reusable helpers
- setup/teardown
- meaningful assertions
- tests actually executed
- failures refined
- required files created
- exactly 8 actual defects documented

Do not stop after creating files. Inspect → Implement → Execute → Fix → Re-run → Refine → Document → Final verification.

START NOW.
